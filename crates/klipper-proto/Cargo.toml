[package]
name = "klipper-proto"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", default-features = false, features = [
    "derive",
    "alloc",
] }
crc = "3.2.1"
heapless = "0.9.3"
libm = "0.2"

[features]
default = ["std"]
std = ["serde/std"]
alloc = []
