[package]
name = "klipper-proto"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
crc = "3.2.1"
heapless = "0.8.0"