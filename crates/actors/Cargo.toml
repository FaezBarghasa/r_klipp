[package]
name = "actors"
version = "0.1.0"
edition = "2024"

[dependencies]
hal = { path = "../hal" }
comms = { path = "../comms" }
heapless = "0.9.3"
embassy-time = { version = "0.5.1", features = ["defmt"] }
kinematics = { path = "../kinematics" }
