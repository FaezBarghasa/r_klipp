[package]
name = "actors"
version = "0.1.0"
edition = "2021"

[dependencies]
hal = { path = "../hal" }
comms = { path = "../comms" }
heapless = "0.7.16"
embassy-time = { version = "0.3.2", features = ["defmt"] }
kinematics = { path = "../kinematics" }
