[package]
name = "automation"
version = "0.1.0"
edition = "2024"

[features]
experimental = []

[dependencies]
heapless = "0.9.3"
nalgebra = { version = "0.35.0", default-features = false, features = ["libm"] }
log = "0.4.34"
