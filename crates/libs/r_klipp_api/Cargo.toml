[package]
name = "r_klipp_api"
version = "0.1.0"
edition = "2021"

[dependencies]
heapless = "0.7"
postcard = { version = "1.0", features = ["heapless"] }
serde = { version = "1.0", features = ["derive"] }
async-trait = "0.1"

[features]
default = []
std = []
