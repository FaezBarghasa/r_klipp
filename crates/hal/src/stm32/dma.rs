// The embassy-stm32 DMA is tightly integrated with the peripherals,
// so a direct implementation of the generic DMA trait is not straightforward.
// The DMA is implicitly used by the peripheral drivers.
