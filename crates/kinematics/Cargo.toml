[package]
name = "kinematics"
version = "0.1.0"
edition = "2021"

[dependencies]
micromath = "2.1.0"
libm = "0.2"
nalgebra = { version = "0.35", default-features = false, features = ["libm"] }
heapless = "0.9.3"
